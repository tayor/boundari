# Raw Python Email Agent

Run this demo from the repository root:

```bash
uv run python examples/raw_python_email_agent/demo.py
```

The internal email is allowed. The external email requires approval and is denied by
the demo callback.
